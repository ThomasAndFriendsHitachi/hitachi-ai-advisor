# Embedding Models

This directory stores local embedding models used for semantic retrieval.

## Default profile

- embedding-hf-bge-m3
- Repo: BAAI/bge-m3
- Local dir: models/embedding_models/bge-m3

## Download

Use the standard downloader with embedding enabled:

python scripts/download_hf_models.py --with-embedding
